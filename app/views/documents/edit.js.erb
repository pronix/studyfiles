$("#document-<%= @file.id %> p").hide();
if ($("#document-<%=@file.id %> form").length == 0) {
$("#document-<%= @file.id %>").append("<%= escape_javascript(render "documents/info_form", :document => @file) %>");
}
