Ваши собеседники
<% current_user.all_senders.each do |sender| %>
  <%= sender.name %>
  <%= current_user.sender_messages(sender).count %> <%= t(:message, :count => current_user.sender_messages(sender).count) %>
<% end %>
