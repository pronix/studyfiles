  <div class="user-details">
    <%= image_tag user.avatar.url(:medium), :class => "avatar" %>

      <h3><%= user.name %></h3>

      <dl class="user-statistics clearfix">
        <dt>Зарегистрирован</dt>
        <dd><%= l user.created_at, :format => "%d %B %Y" %></dd>

        <dt>Последний вход</dt>
        <dd><%= l user.last_sign_in_at, :format => "%d %B %Y" rescue '' %></dd>

        <dt>Загрузил</dt>
        <dd>
          <%= "#{user.documents.count} #{ t(:file, :count => user.documents.count)}" %> на <%= number_to_human_size(user.documents.sum(:item_file_size)) %>
        </dd>

        <dt>Скачал</dt>
        <dd>
          244 файла на 126,1 Мб
        </dd>
      </dl>

      <%= render "rating", :user => user %>

  </div>
  <!-- USER DETAILS BLOCK END -->


<% if user == current_user %>
  <!-- LEGEND START -->
  <div class="yellow-notification notification closable legend">
    <a class="close icon"></a>
    <div>
      <h4>Действия</h4>
      <ul>
        <li>
          Редактировать название и описание
        </li>
        <li>
          Вставить выделенные файлы в эту папку
        </li>
        <li>
          Скачать (можно применять для нескольких выделенных файлов)
        </li>
        <li>
          Создать папку
        </li>
        <li>
          Добавить файлы со своего компьютера
        </li>
        <li>
          Удалить (можно применять для нескольких выделенных файлов)
        </li>
        <li>
          Развернуть всё дерево вкладок
        </li>
        <li>
          Добавить предмет
        </li>
        <li>
          Файлы и папки можно перетаскивать мышкой, схватив их за иконку (можно применять для нескольких выделенных файлов)
        </li>
        <li>
          Чтобы выделить несколько  файлов нажмите на них правой кнопкой мыши
        </li>
      </ul>
    </div>
  </div>
  <!-- LEGEND END -->

  <%= render "shared/news", :news => @news %>

<% else %>

  <%= link_to "Написать в личку", new_discussion_path(:id => @user.id), :class => "button blue-button", :id => "pmme" %>

<% end %>
