$("#folder-<%= @folder.id %> p").hide();
if ($("#folder-<%=@folder.id %> form").length == 0) {
  $("#folder-<%=@folder.id %>").append("<%= escape_javascript(render "folders/form", :folder => @folder) %>");
}
